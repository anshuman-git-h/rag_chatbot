Built an end-to-end RAG pipeline that handles multi-format documents, chunks content, retrieves
relevant context using FAISS, and generates responses with Groq.
Benchmarked semantic chunking + HyDE query expansion against a recursive-chunking baseline using
dual FAISS indexes
